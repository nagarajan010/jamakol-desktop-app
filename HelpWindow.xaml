<Window x:Class="JamakolAstrology.HelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Help" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#f0f0f0" Padding="10">
            <TextBlock x:Name="HeaderTitle" Text="Features" FontSize="18" FontWeight="Bold" Foreground="#333"/>
        </Border>

        <TextBox x:Name="ContentText" Grid.Row="1" Margin="10" 
                 IsReadOnly="True" TextWrapping="Wrap" 
                 VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas, Courier New, Monospace" FontSize="14"
                 BorderThickness="0" Background="Transparent"/>

        <Button Grid.Row="2" Content="Close" Width="100" Height="30" Margin="10" 
                HorizontalAlignment="Right" Click="CloseButton_Click"/>
    </Grid>
</Window>
