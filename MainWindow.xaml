<Window x:Class="JamakolAstrology.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:JamakolAstrology.Controls"
        mc:Ignorable="d"
        Title="Jamakol Astrology - Vedic Birth Chart Calculator" 
        Height="800" Width="1300"
        MinHeight="700" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#fffff0">
    <Window.Resources>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Simple Header Bar -->
        <Border Grid.Row="0" Background="#cc0000" Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="BtnBirthChart" Content="Birth Chart" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="BtnBirthChart_Click"
                            Background="#990000"
                            Padding="15,6"
                            Margin="0,0,10,0"
                            FontSize="14"/>
                    
                    <Button x:Name="BtnJamakol" Content="Jamakol" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="BtnJamakol_Click"
                            Background="#cc0000"
                            Padding="15,6"
                            FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="📤 Export" Click="ExportButton_Click"
                            Background="#FF9800" Foreground="White" 
                            Padding="8,6" Margin="0,0,5,0" FontSize="12"
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                            ToolTip="Export saved charts to file"/>
                    <Button Content="📥 Import" Click="ImportButton_Click"
                            Background="#9C27B0" Foreground="White" 
                            Padding="8,6" Margin="0,0,10,0" FontSize="12"
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                            ToolTip="Import charts from file"/>
                    <Button x:Name="SettingsButton" Content="⚙ Settings" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="SettingsButton_Click"
                            Background="#990000"
                            Padding="10,6"
                            FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Full Screen Tabs -->
        <TabControl x:Name="MainTabControl" Grid.Row="1" Background="#fffff0" BorderThickness="0" Margin="2">
            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <ContentPresenter ContentSource="SelectedContent" />
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
            <!-- Birth Chart Tab -->
            <TabItem Header="Birth Chart" Style="{StaticResource TabItemStyle}">
                <Grid Margin="0,5,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Input Panel Inside Tab -->
                    <controls:BirthInputPanel x:Name="BirthInputControl" Grid.Row="0" CalculateRequested="OnBirthCalculateRequested"/>

                    <!-- All content tabs in a unified TabControl -->
                    <TabControl x:Name="BirthContentTabs" Grid.Row="1" Background="Transparent" BorderThickness="0" SelectionChanged="BirthContentTabs_SelectionChanged" Margin="0,0,0,0">
                        <TabControl.Resources>
                            <Style TargetType="TabItem">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="TabItem">
                                            <Border x:Name="TabBorder" Background="#e0e0e0" CornerRadius="5,5,0,0" 
                                                    Padding="15,6" Margin="0,0,2,0">
                                                <ContentPresenter ContentSource="Header"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="TabBorder" Property="Background" Value="#cc0000"/>
                                                    <Setter Property="Foreground" Value="White"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="FontSize" Value="12"/>
                            </Style>
                        </TabControl.Resources>

                        <!-- Chakras Tab: Full width divisional charts -->
                        <TabItem Header="Chakras">
                            <controls:ChakrasPanel x:Name="ChakrasPanelControl" Margin="0,5,0,0"/>
                        </TabItem>

                        <!-- Basic Tab: Charts Left, Details Right -->
                        <TabItem Header="Basic">
                            <Border Background="#fffff0" BorderBrush="#aaaaaa" BorderThickness="1" Margin="0" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.5*"/> <!-- Side Charts -->
                                        <ColumnDefinition Width="*"/>   <!-- Content -->
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border x:Name="BasicChartPlaceholder" Grid.Column="0" Margin="0,0,5,0"/>

                                    <!-- Basic Tab (from BirthChartDetailsPanel) -->
                                    <controls:BirthChartDetailsPanel x:Name="BirthDetailsPanel" Grid.Column="1" Margin="0"/>
                                </Grid>
                            </Border>
                        </TabItem>
                        
                        <!-- Dashas Tab: Charts Left, Dashas Right -->
                        <TabItem Header="Dashas">
                            <Border Background="#fffff0" BorderBrush="#aaaaaa" BorderThickness="1" Margin="0" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.5*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border x:Name="DashasChartPlaceholder" Grid.Column="0" Margin="0,0,5,0"/>
                                    
                                    <controls:DashasPanel x:Name="DashasPanelControl" Grid.Column="1" Margin="0"/>
                                </Grid>
                            </Border>
                        </TabItem>
                        
                        <!-- Ashtakavarga Tab: Charts Left, AV Right -->
                        <TabItem Header="Ashtakavarga">
                            <Border Background="#fffff0" BorderBrush="#aaaaaa" BorderThickness="1" Margin="0" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.5*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border x:Name="AVChartPlaceholder" Grid.Column="0" Margin="0,0,5,0"/>

                                    <controls:AshtakavargaDetailsPanel x:Name="AVDetailsPanelControl" Grid.Column="1"/>
                                </Grid>
                            </Border>
                        </TabItem>
                        
                        <!-- KP Tab: Charts Left, KP Right -->
                        <TabItem Header="KP">
                            <Border Background="#fffff0" BorderBrush="#aaaaaa" BorderThickness="1" Margin="0" Padding="5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="0.5*"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <Border x:Name="KPChartPlaceholder" Grid.Column="0" Margin="0,0,5,0"/>

                                    <controls:KPDetailsPanel x:Name="KpDetailsPanelControl" Grid.Column="1"/>
                                </Grid>
                            </Border>
                        </TabItem>
                    </TabControl>
                </Grid>
            </TabItem>

            <!-- Jamakol Tab -->
            <TabItem Header="Jamakol" Style="{StaticResource TabItemStyle}">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Input Panel Inside Jamakol Tab -->
                    <controls:JamakolInputBar x:Name="JamakolInputControl" Grid.Row="0"
                                            CalculateRequested="OnJamakolCalculateRequested"
                                            SaveRequested="OnJamakolSaveRequested"
                                            LoadRequested="OnJamakolLoadRequested"
                                            LiveTimerTick="OnJamakolLiveTimerTick"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.15*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#fffff8" CornerRadius="5" Padding="10" Margin="0,0,10,0" BorderBrush="#ddd" BorderThickness="1">
                            <controls:JamakolChart x:Name="JamakolChartControl" MaxWidth="800" MaxHeight="800" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </Border>

                        <Border Grid.Column="1" Background="#fffff8" CornerRadius="5" Padding="10" BorderBrush="#ddd" BorderThickness="1">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    
                                    <!-- Side-by-Side Prasanna and Panchanga -->
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Left: Prasanna Details Special -->
                                        <controls:PrasannaPanel x:Name="PrasannaPanelControl" Grid.Column="0"/>

                                        <!-- Right: Panchanga Details -->
                                        <controls:PanchangaPanel x:Name="PanchangaPanelControl" Grid.Column="2"/>
                                    </Grid>
                                    
                                    <!-- Jama Graha and Planetary Positions Grids -->
                                    <controls:JamakolDataGridsPanel x:Name="DataGridsPanelControl"/>
                                    
                                    <!-- Saved Chart Info Panel (shown when loading saved chart) -->
                                    <controls:SavedChartInfoPanel x:Name="SavedChartInfoPanelControl"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
