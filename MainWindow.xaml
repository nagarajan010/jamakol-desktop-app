<Window x:Class="JamakolAstrology.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:JamakolAstrology.Controls"
        mc:Ignorable="d"
        Title="Jamakol Astrology - Vedic Birth Chart Calculator" 
        Height="800" Width="1300"
        MinHeight="700" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        Background="#fffff0">
    <Window.Resources>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Simple Header Bar -->
        <Border Grid.Row="0" Background="#cc0000" Padding="15,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <Button x:Name="BtnBirthChart" Content="Birth Chart" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="BtnBirthChart_Click"
                            Background="#990000"
                            Padding="15,6"
                            Margin="0,0,10,0"
                            FontSize="14"/>
                    
                    <Button x:Name="BtnJamakol" Content="Jamakol" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="BtnJamakol_Click"
                            Background="#cc0000"
                            Padding="15,6"
                            FontSize="14"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="📤 Export" Click="ExportButton_Click"
                            Background="#FF9800" Foreground="White" 
                            Padding="8,6" Margin="0,0,5,0" FontSize="12"
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                            ToolTip="Export saved charts to file"/>
                    <Button Content="📥 Import" Click="ImportButton_Click"
                            Background="#9C27B0" Foreground="White" 
                            Padding="8,6" Margin="0,0,10,0" FontSize="12"
                            FontWeight="SemiBold" BorderThickness="0" Cursor="Hand"
                            ToolTip="Import charts from file"/>
                    <Button x:Name="SettingsButton" Content="⚙ Settings" 
                            Style="{StaticResource ButtonStyle}" 
                            Click="SettingsButton_Click"
                            Background="#990000"
                            Padding="10,6"
                            FontSize="12"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Full Screen Tabs -->
        <TabControl x:Name="MainTabControl" Grid.Row="1" Background="#fffff0" BorderThickness="0" Margin="10">
            <TabControl.Template>
                <ControlTemplate TargetType="TabControl">
                    <Grid>
                        <ContentPresenter ContentSource="SelectedContent" />
                    </Grid>
                </ControlTemplate>
            </TabControl.Template>
            <!-- Birth Chart Tab -->
            <TabItem Header="Birth Chart" Style="{StaticResource TabItemStyle}">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Input Panel Inside Tab -->
                    <controls:BirthInputPanel x:Name="BirthInputControl" Grid.Row="0" CalculateRequested="OnBirthCalculateRequested"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="0.7*"/>
                            <ColumnDefinition Width="1.3*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Left Column: Charts -->
                        <Grid Grid.Column="0" Margin="0,0,5,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/> <!-- Rasi -->
                                <RowDefinition Height="*"/> <!-- Navamsa -->
                            </Grid.RowDefinitions>
                            
                            <!-- Rasi Chart -->
                            <Border Grid.Row="0" Background="#fffff8" CornerRadius="3" Padding="2" Margin="0,0,0,2" BorderBrush="#ddd" BorderThickness="1">
                                <controls:SouthIndianChart x:Name="ChartControl"/>
                            </Border>

                            <!-- Navamsa Chart -->
                            <Border Grid.Row="1" Background="#fffff8" CornerRadius="3" Padding="2" Margin="0,2,0,0" BorderBrush="#ddd" BorderThickness="1">
                                <controls:SouthIndianChart x:Name="NavamsaChartControl"/>
                            </Border>
                        </Grid>

                        <!-- Right Column: Details -->
                        <Grid Grid.Column="1">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/> <!-- Planetary Details -->
                                <RowDefinition Height="*"/>    <!-- Other Details + Ashtakavarga -->
                            </Grid.RowDefinitions>

                            <!-- Planetary Positions -->
                            <Border Grid.Row="0" Background="#fffff8" CornerRadius="5" Padding="5" Margin="0,0,0,5" BorderBrush="#ddd" BorderThickness="1">
                                <controls:BirthPlanetaryDetails x:Name="BirthPlanetaryDetailsControl"/>
                            </Border>
                            
                            <!-- Bottom Details Area -->
                            <Grid Grid.Row="1" Margin="0,5,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                
                                <!-- Natal Details Placeholder -->
                                <Border Grid.Column="0" Background="#fffff8" CornerRadius="5" Padding="10" Margin="0,0,5,0" BorderBrush="#ddd" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Natal Chart Details" FontWeight="Bold" Foreground="#990000" Margin="0,0,0,5"/>
                                        <TextBlock Text="No content available" Foreground="#aaa" FontStyle="Italic"/>
                                    </StackPanel>
                                </Border>
                                
                                <!-- Ashtakavarga Placeholder -->
                                <Border Grid.Column="1" Background="#fffff8" CornerRadius="5" Padding="10" Margin="5,0,0,0" BorderBrush="#ddd" BorderThickness="1">
                                    <StackPanel>
                                        <TextBlock Text="Ashtakavarga" FontWeight="Bold" Foreground="#990000" Margin="0,0,0,5"/>
                                        <TextBlock Text="No content available" Foreground="#aaa" FontStyle="Italic"/>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>
            </TabItem>

            <!-- Jamakol Tab -->
            <TabItem Header="Jamakol" Style="{StaticResource TabItemStyle}">
                <Grid Margin="0,10,0,0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Input Panel Inside Jamakol Tab -->
                    <controls:JamakolInputBar x:Name="JamakolInputControl" Grid.Row="0"
                                            CalculateRequested="OnJamakolCalculateRequested"
                                            SaveRequested="OnJamakolSaveRequested"
                                            LoadRequested="OnJamakolLoadRequested"
                                            LiveTimerTick="OnJamakolLiveTimerTick"/>

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.15*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Background="#fffff8" CornerRadius="5" Padding="10" Margin="0,0,10,0" BorderBrush="#ddd" BorderThickness="1">
                            <controls:JamakolChart x:Name="JamakolChartControl" MaxWidth="800" MaxHeight="800" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </Border>

                        <Border Grid.Column="1" Background="#fffff8" CornerRadius="5" Padding="10" BorderBrush="#ddd" BorderThickness="1">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <StackPanel>
                                    
                                    <!-- Side-by-Side Prasanna and Panchanga -->
                                    <Grid Margin="0,0,0,10">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="10"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        
                                        <!-- Left: Prasanna Details Special -->
                                        <controls:PrasannaPanel x:Name="PrasannaPanelControl" Grid.Column="0"/>

                                        <!-- Right: Panchanga Details -->
                                        <controls:PanchangaPanel x:Name="PanchangaPanelControl" Grid.Column="2"/>
                                    </Grid>
                                    
                                    <!-- Jama Graha and Planetary Positions Grids -->
                                    <controls:JamakolDataGridsPanel x:Name="DataGridsPanelControl"/>
                                    
                                    <!-- Saved Chart Info Panel (shown when loading saved chart) -->
                                    <controls:SavedChartInfoPanel x:Name="SavedChartInfoPanelControl"/>
                                </StackPanel>
                            </ScrollViewer>
                        </Border>
                    </Grid>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
