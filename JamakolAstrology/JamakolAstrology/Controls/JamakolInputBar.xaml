<UserControl x:Class="JamakolAstrology.Controls.JamakolInputBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:sys="clr-namespace:System;assembly=mscorlib">

    
    <Border Background="#fff8dc" Padding="10,8" BorderBrush="#ddd" BorderThickness="1" CornerRadius="5" Margin="0,0,0,10">
        <WrapPanel VerticalAlignment="Center">
            <TextBlock Text="Name:" Style="{StaticResource TopLabelStyle}"/>
            <TextBox x:Name="NameInput" Style="{StaticResource TopInputStyle}" Width="120" Text="Query"/>

            <TextBlock Text="Date:" Style="{StaticResource TopLabelStyle}"/>
            <DatePicker x:Name="DateInput" Style="{StaticResource DatePickerStyle}" Width="120"
                        SelectedDate="{x:Static sys:DateTime.Now}"/>

            <TextBlock Text="Time:" Style="{StaticResource TopLabelStyle}"/>
            <TextBox x:Name="TimeInput" Style="{StaticResource TopInputStyle}" Width="70" Text="12:00:00"/>
            <Button Content="Now" Click="NowButton_Click"
                    Margin="5,0,0,0" Padding="8,4"
                    Background="#4CAF50" Foreground="White" 
                    FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>
            <Button x:Name="StartStopButton" Content="Start" Click="StartStopButton_Click"
                    Margin="5,0,0,0" Padding="8,4"
                    Background="#2196F3" Foreground="White" 
                    FontWeight="Bold" BorderThickness="0" Cursor="Hand"/>

            <TextBlock Text="Lat:" Style="{StaticResource TopLabelStyle}"/>
            <TextBox x:Name="LatInput" Style="{StaticResource TopInputStyle}" Width="65" Text="13.0827"/>

            <TextBlock Text="Long:" Style="{StaticResource TopLabelStyle}"/>
            <TextBox x:Name="LongInput" Style="{StaticResource TopInputStyle}" Width="65" Text="80.2707"/>

            <TextBlock Text="TZ:" Style="{StaticResource TopLabelStyle}"/>
            <TextBox x:Name="TzInput" Style="{StaticResource TopInputStyle}" Width="45" Text="5.5"/>

            <Button Content="Calculate" Style="{StaticResource ButtonStyle}" 
                    Click="CalculateButton_Click" Margin="15,0,0,0"/>
            
            <Button Content="💾 Save" Click="SaveButton_Click"
                    Margin="5,0,0,0" Padding="10,8"
                    Background="#28A745" Foreground="White" 
                    FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" FontSize="13"/>
            
            <Button Content="📋 Saved" Click="LoadButton_Click"
                    Margin="5,0,0,0" Padding="10,8"
                    Background="#4A90D9" Foreground="White" 
                    FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" FontSize="13"/>
                    
            <TextBlock x:Name="StatusText" Text="" Foreground="#888" 
                       FontSize="11" Margin="15,0,0,0" VerticalAlignment="Center"/>
        </WrapPanel>
    </Border>
</UserControl>
