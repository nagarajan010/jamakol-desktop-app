<UserControl x:Class="JamakolAstrology.Controls.KPDetailsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JamakolAstrology.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="CenteredCell" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <DataGrid x:Name="KPGrid" AutoGenerateColumns="False" CanUserAddRows="False" 
                  HeadersVisibility="Column" GridLinesVisibility="All"
                  Background="White" RowBackground="White" AlternatingRowBackground="#F8F8F8">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Body/Cusp" Binding="{Binding Name}" Width="120" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Nakshatra lord" Binding="{Binding StarLord}" Width="100"/>
                <DataGridTextColumn Header="Sub lord" Binding="{Binding SubLord}" Width="80" 
                                    ElementStyle="{StaticResource CenteredCell}"/>
                <DataGridTextColumn Header="Prati-sub" Binding="{Binding SubSubLord}" Width="80"/>
                <DataGridTextColumn Header="Sookshma-sub" Binding="{Binding SookshmaLord}" Width="100"/>
                <DataGridTextColumn Header="Praana-sub" Binding="{Binding PranaLord}" Width="90"/>
                <DataGridTextColumn Header="Deha-sub" Binding="{Binding DehaLord}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
