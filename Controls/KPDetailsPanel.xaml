<UserControl x:Class="JamakolAstrology.Controls.KPDetailsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JamakolAstrology.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <Grid>
        <DataGrid x:Name="KPGrid" 
                  Style="{StaticResource DataGridStyle}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  CanUserSortColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column" 
                  BorderThickness="0">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#cc0000"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Padding" Value="6,4"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="FontSize" Value="10"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Body/Cusp" Binding="{Binding Name}" Width="Auto"/>
                <DataGridTextColumn Header="Nakshatra Lord" Binding="{Binding StarLord}" Width="Auto"/>
                <DataGridTextColumn Header="Sub Lord" Binding="{Binding SubLord}" Width="Auto" FontWeight="Bold" Foreground="#cc0000"/>
                <DataGridTextColumn Header="Prati-Sub" Binding="{Binding SubSubLord}" Width="Auto"/>
                <DataGridTextColumn Header="Sookshma" Binding="{Binding SookshmaLord}" Width="Auto"/>
                <DataGridTextColumn Header="Prana" Binding="{Binding PranaLord}" Width="Auto"/>
                <DataGridTextColumn Header="Deha" Binding="{Binding DehaLord}" Width="Auto"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
