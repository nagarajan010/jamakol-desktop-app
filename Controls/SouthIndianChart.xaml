<UserControl x:Class="JamakolAstrology.Controls.SouthIndianChart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="400"
             Background="#1a1a2e">
    <UserControl.Resources>
        <Style x:Key="ChartCellStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="#16213e"/>
        </Style>
        <Style x:Key="SignLabelStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#0f3460"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="FontWeight" Value="Light"/>
            <Setter Property="HorizontalAlignment" Value="Right"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="Margin" Value="0,2,4,0"/>
        </Style>
        <Style x:Key="PlanetTextStyle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#eee"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="TextAlignment" Value="Center"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
        </Style>
        <Style x:Key="AscendantStyle" TargetType="Border">
            <Setter Property="BorderBrush" Value="#e94560"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="#1f4068"/>
        </Style>
    </UserControl.Resources>

    <Grid x:Name="ChartGrid" Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Row 0: Pisces(12), Aries(1), Taurus(2), Gemini(3) -->
        <Border x:Name="Cell12" Grid.Row="0" Grid.Column="0" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="12" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets12" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell1" Grid.Row="0" Grid.Column="1" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="1" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets1" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell2" Grid.Row="0" Grid.Column="2" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="2" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets2" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell3" Grid.Row="0" Grid.Column="3" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="3" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets3" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Row 1: Aquarius(11), Center, Center, Cancer(4) -->
        <Border x:Name="Cell11" Grid.Row="1" Grid.Column="0" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="11" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets11" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" Grid.RowSpan="2" 
                Background="#0f3460" BorderBrush="#e94560" BorderThickness="1">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <TextBlock x:Name="ChartTitle" Text="Birth Chart" 
                           Foreground="#e94560" FontSize="14" FontWeight="Bold" 
                           HorizontalAlignment="Center" Margin="0,0,0,5"/>
                <TextBlock x:Name="AscendantLabel" Text="Asc: " 
                           Foreground="#aaa" FontSize="12" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
        <Border x:Name="Cell4" Grid.Row="1" Grid.Column="3" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="4" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets4" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Row 2: Capricorn(10), Center (spanned), Leo(5) -->
        <Border x:Name="Cell10" Grid.Row="2" Grid.Column="0" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="10" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets10" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell5" Grid.Row="2" Grid.Column="3" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="5" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets5" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <!-- Row 3: Sagittarius(9), Scorpio(8), Libra(7), Virgo(6) -->
        <Border x:Name="Cell9" Grid.Row="3" Grid.Column="0" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="9" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets9" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell8" Grid.Row="3" Grid.Column="1" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="8" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets8" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell7" Grid.Row="3" Grid.Column="2" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="7" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets7" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
        <Border x:Name="Cell6" Grid.Row="3" Grid.Column="3" Style="{StaticResource ChartCellStyle}">
            <Grid>
                <TextBlock Text="6" Style="{StaticResource SignLabelStyle}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="0,15,0,0">
                    <TextBlock x:Name="Planets6" Style="{StaticResource PlanetTextStyle}"/>
                </ScrollViewer>
            </Grid>
        </Border>
    </Grid>
</UserControl>
