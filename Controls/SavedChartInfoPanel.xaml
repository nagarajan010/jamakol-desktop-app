<UserControl x:Class="JamakolAstrology.Controls.SavedChartInfoPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="400">
    
    <Border x:Name="MainBorder" Background="#e8f5e9" BorderBrush="#4CAF50" BorderThickness="1" 
            CornerRadius="5" Padding="10" Margin="0,10,0,0">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="📋 Loaded Chart Info" FontSize="13" FontWeight="Bold" Foreground="#2E7D32" Margin="0,0,0,8"/>
                
                <!-- Close Button -->
                <Button Grid.Column="1" Content="✕" Width="20" Height="20" 
                        Background="Transparent" BorderThickness="0" Foreground="#2E7D32" 
                        FontWeight="Bold" Cursor="Hand" Click="CloseButton_Click"
                        ToolTip="Close Info Panel"/>
            </Grid>
            
            <Grid Margin="0,3,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Result:" Foreground="#555" FontSize="11" FontWeight="SemiBold"/>
                <Border Grid.Column="1" x:Name="ResultBorder" Padding="8,2" CornerRadius="3" HorizontalAlignment="Left">
                    <TextBlock x:Name="ResultText" Text="" FontSize="11" FontWeight="SemiBold" Foreground="White"/>
                </Border>
            </Grid>
            
            <Grid Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Category:" Foreground="#555" FontSize="11" FontWeight="SemiBold"/>
                <TextBlock Grid.Column="1" x:Name="CategoryText" Text="" FontSize="11" Foreground="#333"/>
            </Grid>
            
            <Grid Margin="0,6,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Tags:" Foreground="#555" FontSize="11" FontWeight="SemiBold" VerticalAlignment="Top"/>
                <WrapPanel Grid.Column="1" x:Name="TagsPanel"/>
            </Grid>
            
            <StackPanel Margin="0,6,0,0">
                <TextBlock Text="Prediction / Notes:" Foreground="#555" FontSize="11" FontWeight="SemiBold"/>
                <Border Background="White" BorderBrush="#ccc" BorderThickness="1" Padding="8" CornerRadius="3" Margin="0,3,0,0">
                    <TextBlock x:Name="PredictionText" Text="" FontSize="11" Foreground="#333" TextWrapping="Wrap"/>
                </Border>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
