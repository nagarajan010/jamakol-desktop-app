<UserControl x:Class="JamakolAstrology.Controls.JamakolPlanetGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:res="clr-namespace:JamakolAstrology.Resources">
    
    <DataGrid x:Name="DataGridControl" 
              Style="{StaticResource DataGridStyle}"
              AutoGenerateColumns="False"
              HeadersVisibility="Column"
              IsReadOnly="True"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              CanUserSortColumns="False"
              IsSynchronizedWithCurrentItem="False"
              SelectionMode="Single"
              PreviewMouseWheel="DataGrid_PreviewMouseWheel"
              Loaded="DataGrid_Loaded">
        <DataGrid.ColumnHeaderStyle>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#cc0000"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="6,4"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="10"/>
            </Style>
        </DataGrid.ColumnHeaderStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderPlanet}" Binding="{Binding EnglishName, Converter={StaticResource PlanetNameConverter}}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderDegree}" Binding="{Binding DegreeDisplay}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderNakshatra}" Binding="{Binding NakshatraEnglish, Converter={StaticResource NakshatraNameConverter}}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderPada}" Binding="{Binding Pada}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderSign}" Binding="{Binding SignEnglish, Converter={StaticResource SignNameConverter}}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderKaraka}" Binding="{Binding Karaka}" Width="Auto" FontWeight="Bold" Foreground="#006600"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderR}" Binding="{Binding RetroDisplay}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderC}" Binding="{Binding CombustionFlag}" Width="Auto" FontWeight="Bold" Foreground="#cc0000"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderGati}" Binding="{Binding Gati}" Width="Auto"/>
            <DataGridTextColumn Header="{x:Static res:Strings.HeaderSpeed}" Binding="{Binding SpeedDisplay}" Width="Auto"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
