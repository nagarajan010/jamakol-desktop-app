<UserControl x:Class="JamakolAstrology.Controls.JamakolPlanetGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <DataGrid x:Name="DataGridControl" 
              Style="{StaticResource DataGridStyle}"
              AutoGenerateColumns="False"
              HeadersVisibility="Column"
              IsReadOnly="True"
              CanUserAddRows="False"
              CanUserDeleteRows="False"
              CanUserSortColumns="False"
              FontSize="11"
              IsSynchronizedWithCurrentItem="False"
              SelectionMode="Single"
              PreviewMouseWheel="DataGrid_PreviewMouseWheel"
              Loaded="DataGrid_Loaded">
        <DataGrid.ColumnHeaderStyle>
            <Style TargetType="DataGridColumnHeader">
                <Setter Property="Background" Value="#cc0000"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="Padding" Value="6,4"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="10"/>
            </Style>
        </DataGrid.ColumnHeaderStyle>
        <DataGrid.Columns>
            <DataGridTextColumn Header="Planet" Binding="{Binding EnglishName}" Width="Auto"/>
            <DataGridTextColumn Header="Degree" Binding="{Binding DegreeDisplay}" Width="Auto"/>
            <DataGridTextColumn Header="Nakshatra" Binding="{Binding NakshatraEnglish}" Width="Auto"/>
            <DataGridTextColumn Header="Pada" Binding="{Binding Pada}" Width="Auto"/>
            <DataGridTextColumn Header="Sign" Binding="{Binding SignEnglish}" Width="Auto"/>
            <DataGridTextColumn Header="Karaka" Binding="{Binding Karaka}" Width="Auto" FontWeight="Bold" Foreground="#006600"/>
            <DataGridTextColumn Header="R" Binding="{Binding RetroDisplay}" Width="Auto"/>
            <DataGridTextColumn Header="C" Binding="{Binding CombustionFlag}" Width="Auto" FontWeight="Bold" Foreground="#cc0000"/>
            <DataGridTextColumn Header="Gati" Binding="{Binding Gati}" Width="Auto"/>
            <DataGridTextColumn Header="Speed" Binding="{Binding SpeedDisplay}" Width="Auto"/>
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
