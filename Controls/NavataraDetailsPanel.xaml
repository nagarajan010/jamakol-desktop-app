<UserControl x:Class="JamakolAstrology.Controls.NavataraDetailsPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:JamakolAstrology.Controls"
             xmlns:res="clr-namespace:JamakolAstrology.Resources"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style x:Key="RedHeaderStyle" TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#cc0000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="10"/>
        </Style>
    </UserControl.Resources>

    <Grid Background="#FFFFFF">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5">
            <TextBlock Text="Reference Point:" VerticalAlignment="Center" Margin="0,0,5,0" FontWeight="SemiBold"/>
            <ComboBox x:Name="ReferencePlanetCombo" Width="150" SelectionChanged="ReferencePlanetCombo_SelectionChanged" DisplayMemberPath="Name" SelectedValuePath="Id" Height="25"/>
            <CheckBox x:Name="ShowSpecialPointsCheckBox" Content="Show Special Points" Margin="15,0,0,0" VerticalAlignment="Center" Checked="ShowSpecialPointsCheckBox_CheckedChanged" Unchecked="ShowSpecialPointsCheckBox_CheckedChanged"/>
        </StackPanel>

        <DataGrid x:Name="NavataraGrid" Grid.Row="1" 
                  Style="{StaticResource DataGridStyle}"
                  ColumnHeaderStyle="{StaticResource RedHeaderStyle}"
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column" 
                  BorderThickness="0"
                  Margin="0,5,0,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="No" Binding="{Binding Index}" Width="Auto"/>
                <DataGridTextColumn Header="Navatara" Binding="{Binding NavataraName}" Width="Auto" FontWeight="Bold" Foreground="#cc0000"/>
                <DataGridTextColumn Header="Nakshatra" Binding="{Binding NakshatraName}" Width="*"/>
                <DataGridTextColumn Header="Nak Lord" Binding="{Binding NakshatraLord}" Width="Auto"/>
                <DataGridTextColumn Header="DNA Lord" Binding="{Binding DnaLord}" Width="Auto"/>
                <DataGridTextColumn x:Name="ColSpecialPoints" Header="Special Points" Binding="{Binding SpecialPoint}" Width="*" Visibility="Collapsed"/>
                <DataGridTextColumn Header="Planets in Nakshatra" Binding="{Binding PlanetsInNakshatra}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
